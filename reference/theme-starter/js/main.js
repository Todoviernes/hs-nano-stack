/* main.js — hsns starter theme. Keep this file empty by default;
   modules supply their own module.js when interactivity is required. */
(function () {
  // Intentionally empty. Add cross-module behaviors here if needed.
})();
